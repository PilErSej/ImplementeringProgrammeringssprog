fun int sum(int n) =
  if n == 0 then 0
  else sum(n-1) + n

fun int main() =
  let n = read(int) in
  if ((0 < n && n < 3) || (3 < n && n < 6))
  then write(sum(n))
  else let e = write("Invalid input!") in n
